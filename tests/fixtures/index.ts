/**
 * Central fixture export — all test files import from here.
 *
 * Usage:
 *   import { test, expect } from '../fixtures';
 *   // or from deeper:
 *   import { test, expect } from '../../fixtures';
 */
export { test, expect } from './base.fixture';
