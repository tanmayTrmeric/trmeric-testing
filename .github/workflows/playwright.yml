# name: Trmeric E2E Tests

# on:
#   push:
#     branches: [develop, main]
#   pull_request:
#     branches: [develop]
#   schedule:
#     # Nightly regression on QA — 6:00 AM IST (12:30 AM UTC)
#     - cron: '30 0 * * 1-5'
#   workflow_dispatch:
#     inputs:
#       environment:
#         description: 'Target environment'
#         required: true
#         default: 'qa'
#         type: choice
#         options: [dev, qa, eu]
#       suite:
#         description: 'Test suite to run'
#         required: true
#         default: 'smoke'
#         type: choice
#         options: [smoke, platform, visual, user-stories, all]

# env:
#   TRMERIC_AUTH_TOKEN: ${{ secrets.TRMERIC_AUTH_TOKEN }}
#   TRMERIC_BASE_URL: ${{ secrets.TRMERIC_BASE_URL }}

# jobs:

#   # ── Smoke Tests — Run on every push ─────────────────────
#   smoke:
#     if: github.event_name == 'push' || github.event_name == 'pull_request'
#     name: Smoke Tests
#     runs-on: ubuntu-latest
#     timeout-minutes: 10
#     steps:
#       - uses: actions/checkout@v4

#       - uses: actions/setup-node@v4
#         with:
#           node-version: 20
#           cache: npm

#       - name: Install dependencies
#         run: npm ci

#       - name: Install Playwright browsers
#         run: npx playwright install chromium --with-deps

#       - name: Run smoke tests
#         run: TRMERIC_ENV=qa npx playwright test --project=smoke
#         env:
#           TRMERIC_AUTH_TOKEN: ${{ secrets.TRMERIC_AUTH_TOKEN }}

#       - name: Upload smoke report
#         if: always()
#         uses: actions/upload-artifact@v4
#         with:
#           name: smoke-report
#           path: reports/
#           retention-days: 7

#   # ── Full Regression — Nightly or manual trigger ─────────
#   regression:
#     if: github.event_name == 'schedule' || github.event_name == 'workflow_dispatch'
#     name: Full Regression (${{ github.event.inputs.environment || 'qa' }})
#     runs-on: ubuntu-latest
#     timeout-minutes: 30
#     steps:
#       - uses: actions/checkout@v4

#       - uses: actions/setup-node@v4
#         with:
#           node-version: 20
#           cache: npm

#       - name: Install dependencies
#         run: npm ci

#       - name: Install Playwright browsers
#         run: npx playwright install chromium --with-deps

#       - name: Determine suite
#         id: suite
#         run: |
#           SUITE="${{ github.event.inputs.suite || 'all' }}"
#           ENV="${{ github.event.inputs.environment || 'qa' }}"
#           if [ "$SUITE" = "all" ]; then
#             PROJECTS="--project=smoke --project=platform --project=user-stories"
#           else
#             PROJECTS="--project=$SUITE"
#           fi
#           echo "projects=$PROJECTS" >> $GITHUB_OUTPUT
#           echo "env=$ENV" >> $GITHUB_OUTPUT

#       - name: Run regression tests
#         run: TRMERIC_ENV=${{ steps.suite.outputs.env }} npx playwright test ${{ steps.suite.outputs.projects }}
#         env:
#           TRMERIC_AUTH_TOKEN: ${{ secrets.TRMERIC_AUTH_TOKEN }}

#       - name: Upload regression report
#         if: always()
#         uses: actions/upload-artifact@v4
#         with:
#           name: regression-report-${{ steps.suite.outputs.env }}
#           path: reports/
#           retention-days: 30

#   # ── PR Gate — Platform tests on PRs to develop ──────────
#   pr-gate:
#     if: github.event_name == 'pull_request'
#     name: PR Gate — Platform Tests
#     runs-on: ubuntu-latest
#     timeout-minutes: 20
#     steps:
#       - uses: actions/checkout@v4

#       - uses: actions/setup-node@v4
#         with:
#           node-version: 20
#           cache: npm

#       - name: Install dependencies
#         run: npm ci

#       - name: Install Playwright browsers
#         run: npx playwright install chromium --with-deps

#       - name: Run platform + API contract tests
#         run: TRMERIC_ENV=qa npx playwright test --project=platform
#         env:
#           TRMERIC_AUTH_TOKEN: ${{ secrets.TRMERIC_AUTH_TOKEN }}

#       - name: Upload PR report
#         if: always()
#         uses: actions/upload-artifact@v4
#         with:
#           name: pr-gate-report
#           path: reports/
#           retention-days: 14
